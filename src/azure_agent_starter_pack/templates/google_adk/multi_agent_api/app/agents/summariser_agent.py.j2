"""Summariser sub-agent: condenses information into concise summaries."""

from google.adk.agents import Agent

from app.model_config import get_model

summariser_agent = Agent(
    name="summariser_agent",
    model=get_model(),
    description="Summarises long content into concise, actionable summaries.",
    instruction=(
        "You are a summarisation specialist. Take the provided content and "
        "produce a clear, concise summary with key takeaways."
    ),
)
